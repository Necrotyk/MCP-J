[package]
name = "mcp-j-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
landlock = "0.4"
# seccomp-stream = { git = "https://github.com/firecracker-microvm/seccomp-stream" } # Unavailable
nix = { version = "0.29", features = ["fs", "process", "sched"] }
tokio = { version = "1", features = ["full"] }
libc = "0.2"
